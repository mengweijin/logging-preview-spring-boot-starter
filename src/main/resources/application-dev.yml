info:
  app:
    parentGroupId: @project.parent.groupId@
    groupId: @project.groupId@
    artifactId: @project.artifactId@
    version: @project.version@
server:
  port: 9999
spring:
  thymeleaf:
    cache: true
  application:
    name: logging-previewer
logging:
  file:
    name: logs/application.log
  level:
    root: info
    com.github.mengweijin: info